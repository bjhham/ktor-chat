product:
    type: lib
    platforms: [jvm, android]

dependencies:
    - ../chat-core
    - $libs.exposed
    - $libs.exposed.datetime
    - $libs.h2

test-dependencies:
    - $libs.kotlinx.coroutines.test
    - $libs.exposed.jdbc